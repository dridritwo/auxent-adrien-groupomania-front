export type Post = {
    authorName?: string,
    authorId?: string,
    authorAvatarUrl?: string,
    id?: number,
    title?: string,
    text?: string,
    postImageUrl?: string,
    postCreationDate?: Date,
}